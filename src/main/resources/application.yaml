spring:
  messages:
    basename: i18n/messages
  thymeleaf:
    cache: false
  datasource:
    url: jdbc:mysql://localhost:3306/house24
    username: root
    password: ${DATABASE_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  security:
    oauth2:
      client:
        registration:
          github:
            clientId: ${GITHUB_CLIENT_ID}
            clientSecret: ${GITHUB_CLIENT_SECRET}
          google:
            clientId: ${GOOGLE_ID}
            clientSecret: ${GOOGLE_SECRET}
  mail:
    host: smtp.gmail.com
    port: 587
    username: freeideamonthly1@gmail.com
    password: ${SMTP_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com
upload:
  path: C:\Users\NikkiLA\Desktop\SpringProject\images
server:
  port: 8081
cloud:
  aws:
    credentials:
      access-key: AKIARCYP4BMYWLOFSTNG
      secret-key: 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
    region:
      static: us-west-1
    stack:
      auto: false

application:
  bucket:
    name: myhouse24