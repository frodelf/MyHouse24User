spring:
  main:
    allow-bean-definition-overriding: true
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2MB
      max-file-size: 5MB
      max-request-size: 10MB
  datasource:
    url: jdbc:mysql://localhost:3306/house24
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  security:
    oauth2:
      client:
        registration:
          github:
            clientId: ${GITHUB_CLIENT_ID}
            clientSecret: ${GITHUB_CLIENT_SECRET}
  mail:
    host: smtp.gmail.com
    port: 587
    username: kripword@gmail.com
    password: ${SMTP_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com
server:
  port: 8081
cloud:
  aws:
    credentials:
      access-key: AKIAZ6GXNB7ZA2A7WE53
      secret-key: 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
    region:
      static: eu-north-1
    stack:
      auto: false

application:
  bucket:
    name: awss3bucketforavadatest